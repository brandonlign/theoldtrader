name: Retrigger corrected carry recovery
on:
  push:
    branches: [main]
    paths: ['.github/workflows/retrigger-carry-recovery.yml']
permissions:
  actions: write
  contents: read
jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch corrected frozen trials
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh workflow run run-trial26-crossvenue.yml --repo "$GITHUB_REPOSITORY" --ref main
